apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
stringData:                # base64 인코딩 없이 “사람이 읽을 수 있는” 값으로 작성 → 적용 시 자동 인코딩됨
  DB_USER: admin
  DB_PASS: change-me
  # 파일 형태로 마운트할 예시
  credentials.json: |
    { "token": "abc-123", "scope": "demo" }